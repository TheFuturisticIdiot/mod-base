package com.example.mymod.screen;

// Screens are auto-registered by the base. If a menu has no .screen() set,
// a GenericScreen is used automatically. You only need to write a screen class
// for custom rendering like progress bars.

// The crate uses GenericScreen (no screen class needed).
// The crusher uses a custom CrusherScreen for its progress bar.

// See ModMenus.java for how .screen() is used.
// See CrusherScreen.java for an example custom screen.
